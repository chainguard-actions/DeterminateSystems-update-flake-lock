name: Test nix-options passthrough
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up git config
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Create a minimal flake.nix and flake.lock
        run: |
          cat > flake.nix << 'EOF'
          {
            description = "Test flake";
            inputs.nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
            outputs = { self, nixpkgs }: {};
          }
          EOF

          cat > flake.lock << 'EOF'
          {
            "nodes": {
              "nixpkgs": {
                "locked": {
                  "lastModified": 1700000000,
                  "narHash": "sha256-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=",
                  "owner": "NixOS",
                  "repo": "nixpkgs",
                  "rev": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
                  "type": "github"
                },
                "original": {
                  "owner": "NixOS",
                  "ref": "nixos-unstable",
                  "repo": "nixpkgs",
                  "type": "github"
                }
              },
              "root": {
                "inputs": {
                  "nixpkgs": "nixpkgs"
                }
              }
            },
            "root": "root",
            "version": 7
          }
          EOF

          git add flake.nix flake.lock
          git commit -m "Add test flake files"

      - name: Install mock nix binary
        run: |
          mkdir -p "$HOME/.local/bin"
          cat > "$HOME/.local/bin/nix" << 'EOF'
          #!/bin/sh
          echo "Mock nix called with args: $*" >&2
          # Write args to a file for later inspection
          echo "$*" > /tmp/nix_args.txt
          # Simulate nix flake update
          if echo "$*" | grep -q "flake"; then
            touch flake.lock
            git add flake.lock 2>/dev/null || true
            git commit --allow-empty -m "flake.lock: Update" 2>/dev/null || true
          fi
          exit 0
          EOF
          chmod +x "$HOME/.local/bin/nix"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - id: action
        uses: .
        with:
          token: ${{ github.token }}
          nix-options: "--extra-substituters https://cache.nixos.org"
          commit-msg: "flake.lock: Update"
          pr-title: "flake.lock: Update"
          branch: "update_flake_lock_test_nix_options"
        continue-on-error: true

      - name: Assert nix was called with extra options
        run: |
          echo "Checking nix args for extra options..."
          if [ -f /tmp/nix_args.txt ]; then
            NIX_ARGS="$(cat /tmp/nix_args.txt)"
            echo "nix args: $NIX_ARGS"
            # nix-options should be prepended before "flake update"
            if echo "$NIX_ARGS" | grep -q "extra-substituters"; then
              echo "PASS: --extra-substituters option was passed to nix"
            else
              echo "FAIL: --extra-substituters option was not found in: $NIX_ARGS"
              exit 1
            fi
            if echo "$NIX_ARGS" | grep -q "cache.nixos.org"; then
              echo "PASS: cache URL was passed to nix"
            else
              echo "FAIL: cache URL was not found in: $NIX_ARGS"
              exit 1
            fi
          else
            echo "WARN: /tmp/nix_args.txt not found - nix may not have been called"
          fi

      - name: Assert action completed
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "Action outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action completed with outcome: $OUTCOME"
          else
            echo "FAIL: unexpected outcome: $OUTCOME"
            exit 1
          fi
